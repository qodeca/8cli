# Timeline template

A live campaign keeps one `timeline-YYYY-MM-DD.md` per day, append-only, each line starting with
a UTC timestamp read from the clock.

This template is not loaded by the context loader: only `timeline-<date>.md` names are.
