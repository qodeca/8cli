# Parked

Decisions the leader took alone because nobody was reachable, waiting for the owner to confirm or
reverse. `xez-unattended-off` walks this list one entry at a time and empties it.

Format, one per entry:

```
## <YYYY-MM-DD HH:MM> – <what was decided>
Why it could not wait:
What the leader chose:
What it would cost to reverse:
```

No entries yet.
