# Timeline placeholder

Day timelines are named `timeline-<YYYYMMDD>.md`, one per day, and the leader injects only the
newest. This placeholder sorts first so it is never the newest, and it exists so the naming
pattern is visible in a fresh clone. Delete it once a real day timeline exists.
