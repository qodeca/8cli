# SPDX-License-Identifier: GPL-3.0-only
# SPDX-FileCopyrightText: 2026 Qodeca sp. z o.o.

# Keeps SHA-pinned GitHub Actions and npm dependencies current.
# Note: the n8n test image is pinned (tag+digest) in test/e2e/setup/global.ts;
# Dependabot's docker manager only scans Dockerfiles/compose, so that digest is
# bumped manually (or via a Renovate customManager if adopted).
# Work lands on `develop`; `main` takes releases only. GitHub reads this file from the default
# branch, so `target-branch` takes effect once this change reaches `main`.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    target-branch: develop
    schedule:
      interval: weekly
    commit-message:
      prefix: ci

  - package-ecosystem: npm
    directory: /
    target-branch: develop
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
