# Designs

One folder per feature: `designs/<feature>/`. For 8cli a design is the command, its flags, and example output for the normal, empty and error cases. See `SDLC.md` § The design gate.

Smoke test line – this pull request is closed without merging.
